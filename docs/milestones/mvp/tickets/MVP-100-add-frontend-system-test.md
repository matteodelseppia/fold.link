# MVP-100: Add the frontend system test

## Description

Exercise the served page's form-to-result behavior using the selected lightweight browser or DOM-capable system approach.

## Acceptance Criteria

- The test loads `/`, submits a controlled destination, and observes a clickable short link.
- The copy control path is verified with a stubbed or permitted clipboard.
- A server validation error is visibly rendered.
- Testing: the test passes against the packaged application and is traced to F04.
