/**
 * Root package for the fold.link application.
 *
 * <p>Package naming mirrors the reversed domain {@code fold.link} -&gt;
 * {@code link.fold}, matching the Gradle {@code group} coordinate. Feature
 * code is organized in subpackages under this root (for example
 * {@code link.fold.api}, {@code link.fold.domain}, {@code link.fold.redis})
 * as those layers are introduced by later tickets; no feature controllers
 * live directly in the root package.
 */
package link.fold;
