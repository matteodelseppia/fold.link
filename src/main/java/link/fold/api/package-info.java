/**
 * HTTP surface for fold.link: request/response DTOs, the link-creation and redirect controllers,
 * and the {@link link.fold.api.ApiExceptionHandler} translating domain exceptions into the stable
 * error contract from ADR-001.
 */
package link.fold.api;
