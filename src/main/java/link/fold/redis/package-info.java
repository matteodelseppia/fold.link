/**
 * The Redis-backed implementation of {@link link.fold.domain.UrlMappingRepository}: the versioned
 * key codec and the repository adapter itself. No Redis types cross into {@code link.fold.domain}
 * or {@code link.fold.api}.
 */
package link.fold.redis;
